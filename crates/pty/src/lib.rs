//! QuantaTerm PTY management and shell interaction
//!
//! PTY management and shell interaction.

#![warn(missing_docs)]
#![deny(unsafe_code)]

/// Placeholder module for pty
pub struct Pty;

impl Pty {
    /// Create a new instance
    pub fn new() -> Self {
        Self
    }
}

impl Default for Pty {
    fn default() -> Self {
        Self::new()
    }
}
