//! QuantaTerm Terminal block and command grouping
//!
//! Terminal block and command grouping.

#![warn(missing_docs)]
#![deny(unsafe_code)]

/// Placeholder module for blocks
pub struct Blocks;

impl Blocks {
    /// Create a new instance
    pub fn new() -> Self {
        Self
    }
}

impl Default for Blocks {
    fn default() -> Self {
        Self::new()
    }
}
